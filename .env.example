# Environment Configuration Template
# Copy this file to .env and replace the placeholder values with your actual configuration

# Database Configuration
# PostgreSQL connection string for MIMIC-III database
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:port/mimiciii

# Table Configuration
# Name of the table containing admission comorbidity data
# the table should contain one hadm_id and 30 comorbidity categories
# Replace with your actual table name
ADMISSION_COMORBIDITY_TABLE=your_admission_comorbidity_table_name

# Patient Configuration
# Target patient identifier or criteria for analysis
# Age >= 16 and <= 89
# first ICU admission
# Replace with your specific patient criteria or identifier
TARGET_PATIENT=your_target_patient_value

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit the .env file to version control
